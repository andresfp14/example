device: cpu
deterministic: true
threads: 1
epochs: 3
accumulation_steps: 1
optimizer:
  _target_: torch.optim.SGD
  lr: 0.05
  momentum: 0.9
scheduler:
  _target_: torch.optim.lr_scheduler.ReduceLROnPlateau
  mode: min
  factor: 0.1
  patience: 2
