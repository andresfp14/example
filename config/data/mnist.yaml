name: mnist
root: ${paths.data}/datasets
validation_size: 10000
split_seed: 42
batch_size: 128
workers: 0
# Subsets are for quick execution checks, not final scientific comparisons.
train_limit: null
valid_limit: null
